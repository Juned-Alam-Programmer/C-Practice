//write program to check that 97 is divisibal or nor

#include<stdio.h>
#include<conio.h>

int main()
{
    // int num;
    // printf("enter the number=");
    // scanf("%d",&num);
    // printf("divisibility test return %d",num % 97);   
    
    int x=2, y=3, z=3, k=1;
    int result = 3*x/y-z+k;
    printf("the vallue of result is = %d",result);
    return 0;
}