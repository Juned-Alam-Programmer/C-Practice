// Area of circle and the vallue of cyclender

#include <stdio.h>
#include <conio.h>
// the farmula of area of circle is = pi*r*r
int main()

{
    /*float pi, r, a;
     printf("enter the vallue of pi\n");
     scanf("%f", &pi);

     printf("enter the vallue of r\n");
     scanf("%f", &r);

     a = pi * r * r;
     printf("the area of circle is %f", a);*/

    float pi = 3.14;
    int r = 3;
    printf("the area of circle is %f/n", pi * r * r);

    int h = 4;
    printf("the vallue of cyclender is %f/n ", pi * r * r * h);
    return 0;
}